# ── Núcleo da API ──────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
gunicorn==23.0.0              # servidor de produção (Render / Railway)
python-multipart==0.0.12      # formulário de login OAuth2

# ── Banco de dados ─────────────────────────────────────────────────────────
sqlalchemy==2.0.35
pydantic-settings==2.5.2
psycopg2-binary==2.9.9        # driver PostgreSQL (produção); ignorado com SQLite

# ── Autenticação ───────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
bcrypt==4.2.0

# ── Cliente HTTP (Gemini) e testes ─────────────────────────────────────────
httpx==0.27.2

# ── IA (Tutor Inteligente Evolutivo — provedor OpenAI, ver
# app/services/error_explanation_service.py) ────────────────────────────────
openai==2.44.0

# ── Observabilidade ────────────────────────────────────────────────────────
# Sem SENTRY_DSN configurado, o SDK fica "enabled: False" e não manda nada
# (mesmo padrão do frontend, ver src/sentry.js) -- seguro rodar sem conta
# no Sentry em dev/CI.
sentry-sdk==2.64.0

# ── Importador Anki ────────────────────────────────────────────────────────
zstandard==0.25.0             # descomprime collection.anki21b (Anki 23.10+)
nh3==0.3.6                    # sanitização de HTML (binding do Ammonia/html5ever) — não dá pra sanitizar HTML com regex

# ── Suíte de testes isolada (tests/) ────────────────────────────────────────
# Paralela aos scripts test_*.py na raiz (que continuam rodando contra o
# estalo.db local de verdade) — não substitui aquilo, é uma camada nova
# isolada em banco de memória. Ver tests/conftest.py.
pytest==9.1.1
factory_boy==3.3.3
